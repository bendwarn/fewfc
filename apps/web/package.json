{
  "name": "web",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build:rules-wasm && bun run build:nuxt",
    "build:nuxt": "nuxt build",
    "build:rules-wasm": "cd ../.. && cargo build --release --target wasm32-unknown-unknown && mkdir -p apps/web/worker/wasm && cp target/wasm32-unknown-unknown/release/fewfc.wasm apps/web/worker/wasm/fewfc.wasm",
    "cf:dev": "APP_ENV=development bun run build && node node_modules/.bin/wrangler d1 migrations apply fewfc-auth --local && node node_modules/.bin/wrangler dev --env=\"\"",
    "cf:deploy:staging": "bun run validate:deploy staging && APP_ENV=staging bun run build && wrangler deploy --env staging",
    "cf:deploy:production": "bun run validate:deploy production && APP_ENV=production bun run build && wrangler deploy --env production",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply fewfc-auth --local",
    "db:migrate:remote": "wrangler d1 migrations apply fewfc-auth --remote",
    "dev": "APP_ENV=development node node_modules/.bin/wrangler dev",
    "generate": "APP_ENV=production nuxt generate",
    "preview": "APP_ENV=production nuxt preview",
    "postinstall": "APP_ENV=development nuxt prepare",
    "test:e2e": "playwright test",
    "test:e2e:server": "bun tests/e2e/prepare-storage.ts && APP_ENV=development bun run build && bunx wrangler d1 migrations apply fewfc-auth --local --persist-to .wrangler/e2e && bunx wrangler dev --env=\"\" --persist-to .wrangler/e2e --port 8787 --var LOCAL_PASSWORD_RESET_ENABLED:true",
    "typecheck": "APP_ENV=development nuxt typecheck",
    "validate:deploy": "bun validate-deploy-environment.ts"
  },
  "dependencies": {
    "@better-auth/drizzle-adapter": "^1.6.23",
    "better-auth": "^1.6.23",
    "drizzle-orm": "^0.45.2",
    "nuxt": "^4.4.8",
    "vue": "^3.5.39",
    "vue-router": "^5.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "@playwright/test": "^1.61.1",
    "@tailwindcss/vite": "^4.3.2",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.0",
    "drizzle-kit": "^0.31.10",
    "tailwindcss": "^4.3.2",
    "typescript": "^6.0.3",
    "vue-tsc": "^3.3.6",
    "wrangler": "^4.106.0"
  }
}
